"""
环境配置文件
用于配置开发和生产环境的环境变量
"""

# Supabase配置
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_anon_key_here

# 数据库配置
DATABASE_URL=postgresql://user:password@localhost:5432/price_memory

# API配置
API_BASE_URL=http://127.0.0.1:8000
DEBUG=true

# Playwright配置
PLAYWRIGHT_WS_ENDPOINT=ws://43.133.224.11:20001/
BROWSER_MODE=remote

# 节点配置
NODE_NAME=node-local
NODE_REGION=local
NODE_CONCURRENCY=1
NODE_PAUSED=0

# 任务配置
WORKER_TASK_RETRIES=2
AUTO_CONSUME_QUEUE=false

# 代理配置（可选）
# HTTP_PROXY_LIST=http://proxy1:8080,http://proxy2:8080
# PLAYWRIGHT_PROXIES=http://proxy1:8080,http://proxy2:8080

# 汇率配置（可选）
# EXCHANGE_RATES_SOURCE=https://api.exchangerate.host/latest?base=USD
EXCHANGE_RATES_REFRESH_SEC=3600

# SMTP配置（可选）
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM=your_email@gmail.com

# Webhook配置（可选）
# ALERT_WEBHOOK_SECRET=your_webhook_secret

# 其他配置
STRICT_HTTP=0